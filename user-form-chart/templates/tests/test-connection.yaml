apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "user-form-chart.fullname" . }}-test-connection"
  labels:
    {{- include "user-form-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "user-form-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
